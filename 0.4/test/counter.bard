;;; to build standalone:
;;; move this source file to the root of the bard source project
;;; as a sibling to the "bard" collection directory
;;; do:
;;; raco exe counter.bard
;;;
;;; executes in 4 milliseconds when compiled with exe

#lang bard
(require "special.rkt")
(def x 0)
(time (with-exit (return) 
        (repeat 
          (if (> x 1000000) 
            (return x) 
            (set! x (+ x 1))))))
(display x)
