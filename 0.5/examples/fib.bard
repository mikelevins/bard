
(define constant N (limited <fixnum> :minimum 1 :maximum nothing))

(define method (fib n)
  with {n (exactly 1)}
  1)

(define method (fib n)
  with {n (exactly 2)}
  1)

(define method (fib n)
  with {n N}
  (+ (fib (- n 1))
     (fib (- n 2))))


