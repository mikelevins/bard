
(in-package :bard)

(defparameter $env1 
  (extend-env (null-env)
              'x 1
              'y 22
              'z 333))

(compile '(|abort| z) $env1)

(compile '(|and|) $env1)
(compile '(|and| x) $env1)
(compile '(|and| x y) $env1)
(compile '(|and| x y z) $env1)

