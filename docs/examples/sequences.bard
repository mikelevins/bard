(defstruct (pair left right))
(defvector (2tuple 2 nothing))

(defprotocol Consing
  (cons Element (Cons | Element))
  (car Cons)
  (cdr Cons))

(defmethod cons ((hd Anything)(tl Anything))
  (pair hd tl))

(defmethod cons ((hd Anything)(tl Sequence))
  (add-first hd tl))

